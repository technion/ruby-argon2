# frozen_string_literal: true

source 'https://rubygems.org'

gemspec

group :test do
  # TODO: Remove/relax version locks where possible
  # gem 'bundler', '~> 2.0'
  # gem 'minitest', '~> 5.8'
  gem 'rake', '~> 13.0.1'
  gem 'rubocop', '~> 1.7'
  gem 'simplecov', '~> 0.20'
  gem 'simplecov-lcov', '~> 0.8'

  gem 'byebug'
  gem 'faker'
  gem 'rspec'
  gem 'shoulda-matchers'
  # gem 'simplecov', require: false
  # gem 'timecop' # TODO: Unused, remove?
end
